//reverse a string 

function reverseAString(str) {
    //  make str to an array first
    //reverve method (which only works on arrays)
    //then take array of str back to a str 
    return str.split("").reverse().join("");
}
